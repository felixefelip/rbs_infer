# Generated by rbs_infer (devise)
#
# Per-scope Devise controller helpers. Devise class_evals these at
# boot (Devise::Controllers::Helpers.define_helpers), so they are
# invisible to static analysis; the scopes come from `devise_for`
# declarations in config/routes.rb.

module DeviseScopedHelpers
  def current_account: () -> (Account & Account::Validated)?

  def authenticate_account!: (?::Hash[::Symbol, untyped] opts) -> (Account & Account::Validated)

  def account_signed_in?: () -> bool

  def account_session: () -> untyped

  # Receiver narrowed under `authenticate_account!`.
  module AccountAuthenticated
    def current_account: () -> (Account & Account::Validated)

    def account_signed_in?: () -> true
  end
end
