# Adds nothing — which is the point. It inherits both the dispatcher and the
# handler, and its dispatch site is the only evidence `Example65Reporter#handle`
# has.
class Example65CsvReporter < Example65Reporter
end
